# Incident Investigation Copilot

An on-call assistant that retrieves logs, runs sandboxed diagnostics, and
drafts incident reports.

## Status

Phase 0 walking skeleton: health endpoint, minimal LangGraph, and full
build toolchain (`make check`).

## Quick start

```bash
make data
make check
make dev
```
